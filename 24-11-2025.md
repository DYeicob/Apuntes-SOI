## 📌 Preguntas de clase

### **1. ¿Un trabajo se puede ejecutar si no se dispone de memoria principal (MP)?**

**Falso.**
Un trabajo debe estar cargado en la **memoria principal** para poder ejecutarse.
No obstante, **no es necesario cargarlo por completo**, gracias a la **memoria virtual**, que permite mantener en almacenamiento secundario aquellas partes del programa que aún no se necesitan.

---

### **2. Diferencia entre fichero y directorio**

#### **Fichero**

* Contiene **datos del usuario**.
* Es una unidad lógica que almacena información (texto, imágenes, programas, etc.).

#### **Directorio**

* Es una **estructura organizativa** que agrupa ficheros y/o otros directorios.
* Se implementa como una **tabla** que almacena, para cada fichero o subdirectorio:

  * Nombre
  * Propietario
  * Fecha de creación
  * Fecha de último acceso
  * Fecha de última modificación
  * Tamaño en bytes
  * Asignación (en ficheros) o puntero (en directorios)

---

### **3. ¿Los nombres de los directorios deben ser únicos dentro de un mismo directorio?**

**Sí.**
Los nombres deben ser **únicos dentro del mismo directorio**.
En cambio, **pueden repetirse** en directorios distintos, ya que la identificación final se hace mediante la **ruta absoluta**.

---

### **4. ¿Para qué se usa la lista de búsqueda?**

La **lista de búsqueda** se utiliza para localizar ficheros cuyo nombre se especifica **sin ruta completa**.
Si el fichero no está en el **directorio de trabajo actual**, el sistema lo busca en los directorios incluidos en esta lista, en el orden de prioridad establecido.

---

### **5. Si el conjunto de trabajo de un proceso aumenta súbitamente en número de páginas, ¿qué impacto tiene sobre la tasa de fallos de página?**

Un aumento repentino del **conjunto de trabajo** implica que el proceso necesita **más páginas activas**.
Si la memoria asignada no es suficiente para contenerlas:

➡️ **La tasa de fallos de página aumenta de forma notable**,
➡️ pudiendo producirse **thrashing**, ya que el proceso expulsa páginas que vuelve a necesitar inmediatamente.

---
